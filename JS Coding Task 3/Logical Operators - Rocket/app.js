//Take 3 variables

let IsRain = false
let IsSnow = false
let IsSpeedlessthan20km_per_hr=false 

canLaunch = (IsRain == false) && (IsSnow == false) && (IsSpeed < 20)

alert(canLaunch + "Rocket will be Launched")